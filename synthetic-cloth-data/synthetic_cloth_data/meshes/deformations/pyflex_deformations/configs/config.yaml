defaults:
 - deformation: artf
 - _self_

mesh_dir: flat_meshes/TOWEL/dev
output_dir: deformed_meshes/TOWEL/pyflex/dev

num_samples: 10
start_id: 0
debug: True

# no logging for now
hydra:
  run:
    dir: .
  output_subdir: null

